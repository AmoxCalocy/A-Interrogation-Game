# 项目进展 (Progress)

## 已完成的里程碑 (Completed Milestones)
*   **[2026-03-13] 第一阶段：项目脚手架与基础结构**
    *   在 `C:\Godot\TIR` 创建了 Godot 4.3 项目的基础文件夹结构。
    *   配置了 `project.godot`，包括分辨率、拉伸模式和全局单例 (Autoload)。
    *   定义了核心 Resource 类：`EvidenceResource`、`TestimonyResource`、`SuspectResource`。
    *   实现了全局状态管理器 `GameState.gd`，支持压力值、防备心管理及信号系统。
*   **[2026-03-14] 第二阶段：对话系统集成与语气选择**
    *   安装并配置了 Dialogue Manager 插件。
    *   编写了初始对话脚本 `main_dialogue.dialogue`，支持温和/威逼/诱导三种语气。
*   **[2026-03-19] 第三阶段：情报系统（档案夹与证词本）**
    *   构建了 `IntelligencePanel` UI 及其 Tab 切换系统。
    *   实现了证据和证词的动态列表展示与自动更新逻辑。
*   **[2026-03-20] 第四阶段：审讯核心逻辑（攻防博弈）**
    *   实现了“出示证据”按钮及其校验逻辑。
    *   在对话脚本中实现了基于压力值（Stress）和防备心（Defense）的动态判定分支。
    *   解决了 UI 交互冲突（层级与鼠标拦截问题）。
*   **[2026-03-25] 第五阶段：完整基础回路与润色 (MVP 完成)**
    *   实现了 `ResultPanel` 结算面板和游戏重置功能。
    *   完成了从审讯开始到破案重启的完整闭环。
    *   使用 `Tween` 增强了 UI 动效（进度条填充、面板侧滑、按钮反馈）。
*   **[2026-03-26] 音频系统集成与系统稳定性修复**
    *   实现了全局音频单例 `AudioManager.gd`，支持背景音和 SFX 播放。
    *   集成了多个交互 SFX（点击、悬停、冲击、纸张滑动）。
    *   修复了 `UNUSED_SIGNAL` 信号警告及 `DialogueManager` 安全检查逻辑。
*   **[2026-04-02] 防备心过高触发审讯失败功能**
    *   在 `GameState.gd` 中增加了 `case_failed` 信号及 90% 防备心阈值检测。
    *   重构了 `ResultPanel.gd`，支持根据胜负动态切换标题与描述文本。
    *   优化了失败流程的视听同步：失败音效仅在结算面板显现时播放，并实时清理残留对话框。
    *   增加了 `is_game_over` 状态锁，确保结算流程的唯一性和逻辑严全。
*   **[2026-04-05] 多人审讯与嫌疑人切换功能**
    *   重构了 `GameState.gd` 的状态管理，引入 `suspects_state` 字典保存各嫌疑人的进度和数值。
    *   在 `InterrogationUI` 中新增了 `SuspectSelector` 下拉菜单，实现了在多个嫌疑人之间无缝切换。
    *   新增了嫌疑人“李某”的数据资源 `suspect_li.tres` 和其专属对话脚本分支。
*   **[2026-04-06] 视觉资产集成与层级优化**
    *   在 `InterrogationUI` 中集成了背景图与嫌疑人动态立绘系统。
    *   通过“双层 CanvasLayer”架构（VisualsLayer & ControlLayer）彻底解决了视觉素材挡住对话气泡的问题。
*   **[2026-04-18] 对话回放功能实现**
    *   在 `GameState.gd` 中引入了全局对话历史数组 `dialogue_history` 及其存储方法。
    *   在情报面板 `IntelligencePanel` 中新增了“对话回顾”页签。
    *   开发了 `HistoryItem` UI 组件，支持根据说话人角色（警官/嫌疑人）自动着色。
    *   在对话剧本中全面集成了历史记录指令，实现了审讯全过程的文本可回溯性。
*   **[2026-04-23] 完整三人连环审讯流程实现**
    *   根据《剧情概要.md》构建了安某(A)、白某(B)和陈某(C，真凶)的完整审讯逻辑与对话分支。
    *   新增了一整套真实案件相关的 `SuspectResource` 和 `EvidenceResource` (如：聊天记录、尸检报告、废弃学校调查等)。
    *   实现了**跨嫌疑人线索系统**：在 `GameState.gd` 中新增 `has_testimony` 与追问变量跟踪机制，玩家必须从A、B处问出关键线索并记录为 `TestimonyResource`，才能在C的审讯中解锁隐藏追问选项。
    *   强化了真凶破防条件：不仅要求出示决定性证据（学校报告），还强制要求玩家必须先提出相关线索追问，使得审讯更具逻辑连贯性与压迫感。

## 待完成的任务 (Pending Tasks)
*   [ ] 扩展内容
    *   [ ] 进一步丰富多嫌疑人之间的逻辑交织。
    *   [ ] 引入“审讯限时”或“生命值”系统。
    *   [ ] 增加更多场景背景。
    *   [ ] 为嫌疑人添加表情差分立绘。

## 当前关注点 (Current Focus)
*   核心交互功能已非常完善。后续可侧重于案件剧本的深度设计与美术表现的进一步打磨。
